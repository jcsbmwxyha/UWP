<Page
    x:Class="FrameCoordinatesGenerator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FrameCoordinatesGenerator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Center">
            <Button Content="Load" Click="LoadButton_Click"/>
            <TextBlock Text="Load Path:" Margin="20,0,10,0" VerticalAlignment="Center"/>
            <TextBlock Name="LoadPathTextBlock" VerticalAlignment="Center"/>
        </StackPanel>
        <Button Content="Start" Click="StartButton_Click" Margin="10,20,0,0"/>
        <ScrollViewer Grid.Row="2" BorderBrush="Black" BorderThickness="3" MaxHeight="600"
                      Margin="5,10,0,0"
                      VerticalScrollBarVisibility="Visible"
                      HorizontalScrollBarVisibility="Visible"
                      MinZoomFactor="0.1"
                      MaxZoomFactor="50"
                      ZoomMode="Enabled">
            <Grid Name="ImageGrid"/>
        </ScrollViewer>
        <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Center">
            <Button Content="Save" Click="SaveButton_Click"/>
            <TextBlock Text="Save Path:  " Margin="20,0,10,0" VerticalAlignment="Center"/>
            <TextBlock Name="SavePathTextBlock" VerticalAlignment="Center"/>
        </StackPanel>
        <!--<TextBox Name="TargetWidthTextBox" Header="Target Width Pixels" Margin="5,10,0,0" Width="150" HorizontalAlignment="Left"/>
        <TextBox Name="TargetHeightTextBox" Header="Target Height Pixels" Margin="5,10,0,0" Width="150" HorizontalAlignment="Left"/>-->
    </StackPanel>
</Page>

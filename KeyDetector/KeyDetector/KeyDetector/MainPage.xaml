<Page
    x:Class="KeyDetector.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KeyDetector"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="600"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Button Content="Load" Click="LoadButton_Click"/>
            <Button Content="Start" Click="StartButton_Click"/>
            <Button Content="Save" Click="SaveButton_Click"/>
            <TextBlock Name="PathTextBlock" Text="Path: "/>
        </StackPanel>
        <ScrollViewer 
              VerticalScrollBarVisibility="Visible"
              HorizontalScrollBarVisibility="Visible" Grid.Row="1">
            <Grid Name="MainGrid"/>
        </ScrollViewer>
    </Grid>
</Page>
